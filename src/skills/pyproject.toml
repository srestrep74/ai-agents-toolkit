[project]
name = "sdd-skills-sdk"
version = "1.0.0"
description = "Dependencies for SDD Skill scripts"
dependencies = [
    "boto3>=1.34.0",
    "botocore>=1.34.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
